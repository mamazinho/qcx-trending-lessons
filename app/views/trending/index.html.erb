<h2>Essa página mostra as disciplinas mais acessadas no dia</h2>
<p>
    <span>Aqui encontram-se os dados das disciplinas e seus acessos diários, </span>
    <span>caso deseje ver os detalhes dessa disciplina, como seus textos, basta clicar no botão <b>Mostrar detalhes</b></span>
</p>

<% if @questions.length > 10 %>
    <button><%= link_to "Ocultar detalhes", :all => false %></button>
<% end %>

<table>
    <thead>
        <tr>

            <% if @questions.length > 10 %>
                <th>Title</th>
                <th>Content</th>
            <% end %>
            <th>Disciplina</th>
            <th>Acesso diário</th>
        </tr>
    </thead>

    <tbody>
        <% @questions.each_with_index do |question, index| %>
            <tr>
                <% if @questions.length > 10 %>
                    <td><%= question['statement'] %></td>
                    <td><%= question['text'] %></td>
                <% end %>
                <td><%= question['discipline'] %></td>
                <td><%= question['daily_access'] %></td>
            </tr>
        <% end %>
    </tbody>
</table>

<% if @questions.length <= 10 %>
    <button><%= link_to "Mostrar detalhes", :all => true %></button>
<% end %>